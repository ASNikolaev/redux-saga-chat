# redux-saga-chat

  Chat on Node.js, Express, React, Redux, Redux-saga, ES6-ES7

Usage
-----

Install:

```bash
  npm install
```

Run Server:

```bash
  npm run api
```

Run React:

```bash
  npm start
```
